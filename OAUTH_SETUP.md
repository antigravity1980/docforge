# 🔐 Чек-лист настройки Google OAuth

Код на вашем сайте правильный. Проблема кроется в настройках Google Cloud или Supabase.

Пожалуйста, внимательно выполните эти 3 шага.

## Шаг 1: Google Cloud Console (Самое важное)

1. Перейдите в [Google Cloud Console > Credentials](https://console.cloud.google.com/apis/credentials).
2. Нажмите на ваш **OAuth 2.0 Client ID** (Web client / Веб-клиент).
3. Найдите раздел **"Authorized redirect URIs"** (Разрешенные URI перенаправления).
4. **ТАМ ДОЛЖНА БЫТЬ ТОЛЬКО ЭТА ССЫЛКА:**

   ```
   https://fihongrqfpcuktsviugd.supabase.co/auth/v1/callback
   ```

   > ❌ **НЕПРАВИЛЬНО:** `https://docforge.site/api/auth/callback` (НЕ вставляйте сюда ссылку на ваш сайт!)
   > ✅ **ПРАВИЛЬНО:** Ссылка должна вести на ваш проект в Supabase.

## Шаг 2: Панель Supabase (Белый список перенаправлений)

1. Перейдите в [Supabase Dashboard > Authentication > URL Configuration](https://supabase.com/dashboard/project/fihongrqfpcuktsviugd/auth/url-configuration).
2. Найдите опцию **"Redirect URIs"**.
3. Там должны быть перечислены **ВСЕ** эти ссылки:

   - `http://localhost:3000/api/auth/callback`
   - `https://docforge.site/api/auth/callback`
   - `https://docforge.site`
   - `https://www.docforge.site/api/auth/callback`
   - `https://www.docforge.site`

4. Нажмите **Save** (Сохранить).

## Шаг 3: Настройки провайдера в Supabase

1. Перейдите в [Supabase Dashboard > Authentication > Providers > Google](https://supabase.com/dashboard/project/fihongrqfpcuktsviugd/auth/providers).
2. Убедитесь, что **"Enable Sign in with Google"** включено (ON).
3. Убедитесь, что **Client ID** и **Client Secret** (Секрет клиента) совпадают с тем, что указано в Google Cloud Console.
4. Если меняли ключи, вставьте их заново и сохраните.

## Как это работает?

1. Ваш сайт отправляет пользователя в -> **Supabase**
2. Supabase авторизует через -> **Google** (по ссылке из Шага 1)
3. Google возвращает пользователя в -> **Supabase**
4. Supabase возвращает пользователя на -> **Ваш сайт** (по ссылке из Шага 2)

Если ссылка в Шаге 1 или Шаге 2 указана неверно, процесс прерывается, и вас выкидывает на страницу входа.
